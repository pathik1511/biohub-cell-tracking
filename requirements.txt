# Core (inference + local eval)
numpy>=1.24
scipy>=1.10
pandas>=2.0
zarr>=3.0
blosc2>=2.5

# Deep-learning detector (training on local RTX 5070 / Kaggle GPU).
# RTX 5070 is Blackwell (sm_120): install a CUDA-12.8 build, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.7

# Tooling
kaggle>=1.6
tqdm>=4.66
